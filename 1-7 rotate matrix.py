def solution1(M, rotate):
    # it's a simple brute force solution where we copy each colum and add it to the new matrix
    # and we keep doing that till we have done all columns


    ans = []
    for i in range(range(M[0]))